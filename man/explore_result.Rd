% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explore_result.R
\name{explore_result}
\alias{explore_result}
\title{Shiny explorer for a GA result}
\usage{
explore_result(result, area)
}
\arguments{
\item{result}{The output of \code{\link{genetic_algorithm}}}

\item{area}{Site polygon (\code{sf}, SpatialPolygons, or coordinate matrix).
Must be projected in metres.}
}
\value{
The Shiny app object, invisibly. Called for its side effect.
}
\description{
One-page viewer for a \code{\link[=genetic_algorithm]{genetic_algorithm()}} result: Leaflet
map of that generation's best layout, and one plotly figure with
fitness, operator rates and population (subplots). New fitness
maxima are marked; click a marker to jump to that generation.
The cell heatmap is not included: it rebuilds
the site grid on every draw and adds little next to the map.
Use \code{\link{plot_cell_heatmap}} or \code{\link{plot_generation}} for that offline.
Requires Suggests \code{shiny}. Plotly is used when installed.
}
\examples{
\dontrun{
explore_result(resultrect, sp_polygon)
}
}
