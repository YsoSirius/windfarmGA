% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_result}
\alias{plot_result}
\title{Plot the best results}
\usage{
plot_result(
  result,
  area,
  best = 1,
  plot_en = 1,
  terrain = FALSE,
  plot_grid = TRUE,
  ccl_roughness = NULL,
  ccl = NULL,
  weibull_src = NULL
)
}
\arguments{
\item{result}{The output of \code{\link{genetic_algorithm}}}

\item{area}{Site polygon (\code{sf}, SpatialPolygons, or coordinate matrix).
Must be projected in metres.}

\item{best}{How many distinct best layouts to draw. Default is 1.}

\item{plot_en}{A numeric value that indicates if the best energy or efficiency
output should be plotted. \code{1} plots the best energy solutions
and \code{2} plots the best efficiency solutions}

\item{terrain}{Draw terrain rasters for the best layout. Reuses
\code{result$terrainModel} when present; \code{TRUE} downloads only if
nothing is stored. A DEM raster rebuilds the model.}

\item{plot_grid}{If \code{TRUE} (default) the used grid is added. You can also
pass another Simple Feature object}

\item{ccl_roughness}{Path to the CLC legend CSV (\code{Rauhigkeit_z} column).}

\item{ccl}{Path to a Corine Land Cover raster when \code{terrain} is on.}

\item{weibull_src}{\code{list(k, a)} shape and scale rasters (e.g. Global
Wind Atlas \code{combined-Weibull-k} / \code{combined-Weibull-A}).}
}
\value{
Returns a data.frame of the best (energy/efficiency) individual
during all iterations
}
\description{
Draw the best layout(s) on the site. Turbine labels are the
total wake in percent. Default is the single best energy layout.
}
\examples{
\dontrun{
## Add some data examples from the package
library(sf)
area <- sf::st_as_sf(sf::st_sfc(
  sf::st_polygon(list(cbind(
    c(4498482, 4498482, 4499991, 4499991, 4498482),
    c(2668272, 2669343, 2669343, 2668272, 2668272)
  ))),
  crs = 3035
))

## Plot the results of a hexagonal grid optimization
plot_result(resulthex, area, best = 1, plot_en = 1, terrain = FALSE)

## Plot the results of a rectangular grid optimization
plot_result(resultrect, area, best = 1, plot_en = 1, terrain = FALSE)
}
}
\seealso{
Other Plotting Functions: 
\code{\link{generation_layouts}()},
\code{\link{plot_cell_heatmap}()},
\code{\link{plot_cloud}()},
\code{\link{plot_development}()},
\code{\link{plot_evolution}()},
\code{\link{plot_generation}()},
\code{\link{plot_parkfitness}()},
\code{\link{plot_population}()},
\code{\link{plot_windfarmGA}()},
\code{\link{plot_windrose}()},
\code{\link{population_census}()},
\code{\link{random_search_single}()}
}
\concept{Plotting Functions}
