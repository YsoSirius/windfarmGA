% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/random_search.R
\name{random_search}
\alias{random_search}
\title{Randomize the output of the Genetic Algorithm}
\usage{
random_search(
  result,
  area,
  runs = 20,
  best = 1,
  plot = FALSE,
  max_dist = 2.2,
  terrain = NULL,
  weibull = NULL,
  weibull_src = NULL,
  ccl = NULL,
  ccl_roughness = NULL
)
}
\arguments{
\item{result}{The resulting matrix of the function \code{\link{genetic_algorithm}}}

\item{area}{Site polygon (\code{sf}, SpatialPolygons, or coordinate matrix).
Must be projected in metres.}

\item{runs}{How many jittered layouts to try per \code{best} start.
Default is 20.}

\item{best}{How many distinct best layouts to refine. Default is 1.}

\item{plot}{Draw the random-search layouts}

\item{max_dist}{A numeric value multiplied by the rotor radius to perform
collision checks. Default is \code{2.2}}

\item{terrain}{\code{NULL} (default) follows the GA and reuses
\code{result$terrainModel}. \code{TRUE} downloads only if nothing is stored.
A DEM rebuilds the model. \code{FALSE} skips terrain.}

\item{weibull}{\code{NULL} follows the GA flag. Weibull rasters are not
stored; pass \code{weibull_src} (or a speed raster as \code{weibull}) again.
Giving \code{weibull_src} is enough; you do not also need \code{weibull = TRUE}.}

\item{weibull_src}{\code{list(k, a)} shape and scale rasters (e.g. Global
Wind Atlas \code{combined-Weibull-k} / \code{combined-Weibull-A}).}

\item{ccl}{Path to a Corine Land Cover raster when \code{terrain} is on.}

\item{ccl_roughness}{Path to the CLC legend CSV (\code{Rauhigkeit_z} column).}
}
\value{
Returns a list.
}
\description{
Jitter the best GA layouts inside their grid cells and
re-evaluate energy. Use this as a short post-search after
\code{\link[=genetic_algorithm]{genetic_algorithm()}}. Terrain and Weibull follow the GA flags
when \code{terrain} / \code{weibull} are \code{NULL}. Terrain rasters from the GA
are reused when stored in \code{result}; pass a DEM to rebuild. Weibull
rasters are not stored; pass \code{weibull_src} again if needed.
}
\examples{
\donttest{
new <- random_search(resultrect, sp_polygon, runs = 20, best = 4)
plot_random_search(resultRS = new, result = resultrect, area = sp_polygon, best = 2)
}
}
\seealso{
Other Randomization: 
\code{\link{plot_random_search}()},
\code{\link{random_search_single}()}
}
\concept{Randomization}
