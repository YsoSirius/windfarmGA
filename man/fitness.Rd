% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitness.R
\name{fitness}
\alias{fitness}
\title{Evaluate the Individual Fitness values}
\usage{
fitness(
  population,
  reference_height,
  rotor_height,
  surface_roughness,
  area,
  rotor,
  wind,
  elevation = NULL,
  terrain = FALSE,
  ccl_raster = NULL,
  weibull = FALSE,
  parallel = FALSE,
  n_cluster = 2
)
}
\arguments{
\item{population}{A list of individuals (layouts with X/Y and cell IDs).}

\item{reference_height}{Height at which \code{wind$ws} was measured.}

\item{rotor_height}{Hub height in metres.}

\item{surface_roughness}{Roughness length in metres. Per-cell when
\code{terrain} is on.}

\item{area}{Site polygon (\code{sf}, SpatialPolygons, or coordinate matrix).
Must be projected in metres.}

\item{rotor}{Rotor radius in metres.}

\item{wind}{Wind data as returned by \code{\link[=windata_format]{windata_format()}} (\code{list(df, probab)}).}

\item{elevation}{Terrain list from \code{\link[=terrain_model]{terrain_model()}} (elevation, orography,
roughness). Unused when \code{terrain} is \code{FALSE}.}

\item{terrain}{Terrain model (elevation + land cover). \code{TRUE}
downloads a DEM via \code{elevatr}. Pass a DEM raster to skip the
download. Per-cell values are computed once and stored in the
result as \code{terrainModel} for \code{\link[=plot_result]{plot_result()}} / \code{\link[=random_search]{random_search()}}.}

\item{ccl_raster}{Land-cover roughness raster from \code{\link[=terrain_model]{terrain_model()}}.}

\item{weibull}{Raster of estimated wind speeds, or \code{FALSE}.}

\item{parallel}{Parallel fitness (\code{parallel} + \code{doParallel}).}

\item{n_cluster}{Worker count when \code{parallel} is \code{TRUE}.}
}
\value{
Returns a list with every individual, consisting of X & Y
coordinates, rotor radii, the runs and the selected grid cell IDs, and the
resulting energy outputs, efficiency rates and fitness values.
}
\description{
The fitness of all individuals in the current population
is calculated after their energy output has been evaluated in
\code{\link{calculate_energy}}. This function reduces the resulting energy
outputs to a single fitness value for each individual.
}
\examples{
\donttest{
## Create a random rectangular shapefile
library(sf)
area <- sf::st_as_sf(sf::st_sfc(
  sf::st_polygon(list(cbind(
    c(4498482, 4498482, 4499991, 4499991, 4498482),
    c(2668272, 2669343, 2669343, 2668272, 2668272)
  ))),
  crs = 3035
))

## Create a uniform and unidirectional wind data.frame and plots the
## resulting wind rose
## Uniform wind speed and single wind direction
wind <- data.frame(ws = 12, wd = 0)
# windrosePlot <- plot_windrose(data = wind, spd = wind$ws,
#                dir = wind$wd, dirres=10, spdmax=20)

## Calculate a Grid and an indexed data.frame with coordinates and
## grid cell IDs.
Grid1 <- grid_area(area = area, size = 200, prop = 1)
Grid <- Grid1[[1]]
AmountGrids <- nrow(Grid)

wind <- list(wind, probab = 100)
startsel <- init_population(Grid, 10, 20)
fit <- fitness(
  population = startsel, reference_height = 100, rotor_height = 100,
  surface_roughness = 0.3, area = area, rotor = 20,
  wind = wind, terrain = FALSE, parallel = FALSE
)
}
}
\seealso{
Other Genetic Algorithm Functions: 
\code{\link{crossover}()},
\code{\link{genetic_algorithm}()},
\code{\link{init_population}()},
\code{\link{mutation}()},
\code{\link{selection}()},
\code{\link{set_crossover}()},
\code{\link{swap_mutation}()},
\code{\link{trimton}()}
}
\concept{Genetic Algorithm Functions}
