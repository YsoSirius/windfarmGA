% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mutation.R
\name{swap_mutation}
\alias{swap_mutation}
\title{Swap mutation of turbine layouts}
\usage{
swap_mutation(ids, grid_ids, p, seed = NULL, min_swaps = NULL, visit = NULL)
}
\arguments{
\item{ids}{Integer matrix with \code{n} rows (turbines) and one column per
individual}

\item{grid_ids}{All valid grid cell IDs}

\item{p}{Mutation probability per turbine}

\item{seed}{Set a seed for comparability. Default is \code{NULL}}

\item{min_swaps}{Minimum number of swaps per individual. Default is
\code{getOption("windfarmGA.min_swaps")} (1)}

\item{visit}{Named visit counts per grid ID. Free cells with fewer visits
are more likely to be chosen. Default is \code{NULL} (uniform)}
}
\value{
Integer matrix of unique grid IDs, same dimension as \code{ids}
}
\description{
Replace occupied grid cells with unused ones. The number of
swaps is \code{max(min_swaps, Binomial(n, p))}, so each individual explores at
least \code{min_swaps} new cells (default 1). The number of turbines stays \code{n}.
}
\examples{
ids <- cbind(c(1, 3, 5, 7), c(2, 4, 6, 8))
swap_mutation(ids, grid_ids = 1:20, p = 0.5, seed = 1)
}
\seealso{
Other Genetic Algorithm Functions: 
\code{\link{crossover}()},
\code{\link{fitness}()},
\code{\link{genetic_algorithm}()},
\code{\link{init_population}()},
\code{\link{mutation}()},
\code{\link{selection}()},
\code{\link{set_crossover}()},
\code{\link{trimton}()}
}
\concept{Genetic Algorithm Functions}
