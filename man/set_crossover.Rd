% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crossover.R
\name{set_crossover}
\alias{set_crossover}
\title{Set crossover of turbine layouts}
\usage{
set_crossover(
  ids,
  grid_ids,
  uplimit = 300,
  seed = NULL,
  verbose = FALSE,
  p_inject = NULL,
  grid_xy = NULL,
  visit = NULL,
  p_spatial = NULL
)
}
\arguments{
\item{ids}{Integer matrix with \code{n} rows (turbines) and one column per parent}

\item{grid_ids}{All valid grid cell IDs}

\item{uplimit}{Maximum number of children. Default is 300}

\item{seed}{Set a seed for comparability. Default is \code{NULL}}

\item{verbose}{If \code{TRUE}, print the number of children}

\item{p_inject}{Fraction of non-shared slots filled from unused grid cells.
Default is \code{getOption("windfarmGA.crossover_inject")} (0.3). At least one
unused cell is injected when any are available.}

\item{grid_xy}{Optional matrix/data.frame with columns \code{ID}, \code{X}, \code{Y}. If
given, a spatial half-plane crossover is used with probability
\code{p_spatial}.}

\item{visit}{Named visit counts per grid ID (undersampled cells preferred)}

\item{p_spatial}{Probability of spatial (vs set) crossover when \code{grid_xy}
is given. Default is \code{getOption("windfarmGA.spatial_crossover")} (0.5)}
}
\value{
Integer matrix of unique grid IDs (\code{n} × children)
}
\description{
Combine two layouts of \code{n} unique grid-cell IDs. Shared sites
are kept; remaining sites are sampled from the parents' exclusive cells
and, with rate \code{p_inject}, from grid cells that neither parent uses.
Identical parents still get unused cells injected so the search does not
freeze. Every child has exactly \code{n} turbines.
}
\examples{
ids <- cbind(c(1, 3, 5, 7), c(1, 4, 5, 9))
set_crossover(ids, grid_ids = 1:20, uplimit = 4, seed = 1)
}
\seealso{
Other Genetic Algorithm Functions: 
\code{\link{crossover}()},
\code{\link{fitness}()},
\code{\link{genetic_algorithm}()},
\code{\link{init_population}()},
\code{\link{mutation}()},
\code{\link{selection}()},
\code{\link{swap_mutation}()},
\code{\link{trimton}()}
}
\concept{Genetic Algorithm Functions}
