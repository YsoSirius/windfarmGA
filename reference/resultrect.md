# A resulting matrix of `genetic_algorithm` with 200 iterations and a rectangular grid derived from `sp_polygon`

A resulting matrix of `genetic_algorithm` with 200 iterations and a
rectangular grid derived from `sp_polygon`

## Usage

``` r
resultrect
```

## Format

An object of class `matrix` (inherits from `array`) with 200 rows and 13
columns.
