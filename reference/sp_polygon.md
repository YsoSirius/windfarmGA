# The rectangular POLYGON used to create `resultrect` & `resulthex`

The rectangular POLYGON used to create `resultrect` & `resulthex`

## Usage

``` r
sp_polygon
```

## Format

An object of class `sf` (inherits from `data.frame`) with 1 rows and 1
columns.
