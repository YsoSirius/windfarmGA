# A resulting matrix of `genetic_algorithm` with 10 iterations and a hexagonal grid derived from `sp_polygon`

A resulting matrix of `genetic_algorithm` with 10 iterations and a
hexagonal grid derived from `sp_polygon`

## Usage

``` r
resulthex
```

## Format

An object of class `matrix` (inherits from `array`) with 10 rows and 13
columns.
