context("User-Interaction")
library(testthat)
library(windfarmGA)

# options(windfarmGA.connection = stdin())
# 
# marryme <- function() {
#   ans <- readLines(con = getOption("windfarmGA.connection"), n = 1)
#   cat("\n")
#   return(ans == "y")
# }


test_that("Test User interaction Functions", {
  # f <- file()
  # options(windfarmGA.connection = f)
  # ans <- paste(c("R"), collapse = "\n") # set this to the number of tests you want to run
  # write(ans, f)
  # 
  # expect_true(readinteger() == "RAN")
  # # reset connection
  # options(windfarmGA.connection = stdin())
  # # close the file
  # close(f)
  
})